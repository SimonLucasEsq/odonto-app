<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New work order item</h1>

  <%= render "form", work_order_item: @work_order_item %>

  <%= link_to "Back to work order items", work_order_work_order_items_path(@work_order), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>

